# Genoray Device Friend Test v12

React + Vite mobile-first interactive prototype.

## Run on Windows
Double-click `start.cmd`, or run:

```cmd
npm install --registry=https://registry.npmjs.org/
npm run dev
```

Open `http://localhost:5173`.

## Vercel
- Framework: Vite
- Build command: `npm run build`
- Output directory: `dist`

## v12 updates
- Quiz background overlap fixed
- Result character images switched to crisp NPC assets
- Observation log changed to notebook/pencil motif
- Result information cards redesigned for blue theme
- Team mission cards redesigned for blue theme
- Team character motion effects enriched
